# A09 — acta de integración y ensayo dev/staging

Fecha de corte: 2026-09-25 UTC. Este documento registra evidencia técnica; la
autorización de producción y la aceptación de Royal Plast/SK Performance siguen
siendo decisiones del responsable y de los operadores.

## Candidato y publicación comprobada

| Superficie | SHA / artefacto | Resultado |
| --- | --- | --- |
| Backend dev | `develop@c7bc43597743206887e5f0c0ec941903e9ed879e`, imagen `sha256:9c8a6718342726eed49ab32ce1c43704f36a1d553eb7aa2220c2c82941d8bc4e` | La [CI y el despliegue](https://github.com/GenaoKing/pos_fifo_system/actions/runs/36086095085) pasaron, health 200 y revisión lista `posfifo-dev-api--0000068`. |
| Portal dev | `develop@280ce68e82782e8fb10908f6266e3189fff67d06` | CI, SWA, login y administración básica verdes. |
| Backend staging | `staging@86adf7ff56c0b85b7a2a0a458244a52185eefe24`, imagen `sha256:247aa585e4623624a07bac2d8f343124dc08f4efbbbc21c855663f0d1355d4c9` | La [promoción #28](https://github.com/GenaoKing/pos_fifo_system/pull/28), [CI/despliegue 36087732831](https://github.com/GenaoKing/pos_fifo_system/actions/runs/36087732831), migraciones y ciclo de notificaciones pasaron; health 200, revisión lista `posfifo-staging-api--0000018`, 155 migraciones en control y ambos tenants. |
| Portal staging | `staging@d66e8d95b0752661f77e3b03e6ba501a442913ad`, bundle `index-CNxwArTV.js` SHA-256 `9C639BA8C66275DAC149CD986CB499BCB4944838AE992EF7060DFE551E1C85EA` | CI/SWA [run 36078162811](https://github.com/GenaoKing/pos-cloud-dashboard/actions/runs/36078162811), HTTP 200 y bundle dirigido a la API staging. |
| Correcciones A09 posteriores | Backend [PR #27](https://github.com/GenaoKing/pos_fifo_system/pull/27), integrado en `c7bc43597743206887e5f0c0ec941903e9ed879e` | Auditoría CT-01 de catálogo y clientes portal, username RBAC sin distinción de mayúsculas y gate físico independiente de secuencias; CI completa verde. |

La rama `main` y el entorno de producción no se modificaron. El ZIP Windows
del código `86adf7ff56c0` se construyó dos veces, en laboratorios separados,
desde `git archive` y un wheelhouse de 36 archivos resuelto con
`--no-index --require-hashes`: ambos ZIP de 37.227.239 bytes dieron SHA-256
`D852CF4C9B90871A4435E42F76B24D44CBD899B6E40D33C74D610355D509DFE1`.
Está bajo `C:\Proyectos\_lab_a09_rc_86adf7f_probe\paquete` y sigue marcado
**CANDIDATO LOCAL — NO PROMOTABLE**. Un commit documental posterior en staging
cambia el SHA y exige reconstruir/volver a comparar el ZIP final.
Por separado, el manifiesto OCI de CI dev para `c7bc435` se descargó del
[run 36086095085](https://github.com/GenaoKing/pos_fifo_system/actions/runs/36086095085)
y pasó `release_manifest.py --verify --assert-git-source --require-promotable`
contra un checkout limpio Windows del SHA exacto. Acredita el artefacto OCI
por digest, no las copias actuales de las tiendas ni el paquete Windows.
El manifiesto OCI de staging del
[run 36087732831](https://github.com/GenaoKing/pos_fifo_system/actions/runs/36087732831)
pasó la misma verificación contra `staging@86adf7f` en otro checkout limpio.

## Datos, restauración y rig cloud/POS

Antes de migrar staging se respaldaron `pos_fifo_staging`,
`tnt_staging_demo` y `tnt_staging_royalplast`; también se respaldaron después
de reparar una tabla huérfana de sucursales en control. Los tres dumps
post-reparación pasaron `pg_restore --list` y restauración completa en bases
locales aisladas; `migrate_cloud` pasó 2/2 y una segunda ejecución no añadió
cambios. Evidencia local bajo
`C:\Proyectos\_lab_a09_rig_20260924\staging_backup_postrepair_20260924`:
SHA-256 control `22F581D22847E02F6D58ECFF9777382973426AF64167665B9E2E3C5840DBE351`,
demo `635E3429D0CA4A3D55397A3BB4446B8D85A6630E58B44BCE1FBF5D01099DD5D6`,
royalplast `909DCE8384B5C3A0C85E31D6ABD07047F8DE6C092894FED28F27653BACB269E8`.

El responsable autorizó `staging_demo` y también `staging_royalplast` para el
rig de staging. Solo se escribieron datos sintéticos en `staging_demo`; el
token de `staging_royalplast` vio su sucursal `01`, **0 productos** y recibió
403 en administración. El portal creó 1 categoría + **273 productos**
sintéticos en demo. Un POS local limpio y migrado en
`pos_a09_staging_pos_clean_20260925` descargó 273/273 por HTTP; el siguiente
ciclo descargó 0 y conservó 273. El pull RBAC inicialmente dejó diferida una
cuenta de servicio remota `STG-01` porque el alta local la canonizó a
`stg-01`; la corrección del PR #27 la resolvió: 2 asignaciones y 0 diferidos
pendientes, sin cursores bloqueados.

Chrome headless abrió el portal SWA publicado, inició sesión con el usuario
administrador de `staging_demo` y cargó las pantallas `/productos` y
`/clientes`; ambas mostraron los registros sintéticos reales de la API. Las
capturas `staging_products_screen.png` y `staging_clients_screen.png` están
en el laboratorio local, fuera de Git.

En otra BD **local desechable**, clonada del POS limpio, Chrome headless abrió
la pantalla POS con 273 productos descargados y un lote FIFO sintético de 20
unidades. Hizo una venta de contado de 10.00 en modo standalone (la primera
corrida tenía `SUCURSAL_CODIGO=SD-001`, sin resolución), luego corrigió el
entorno a `SUCURSAL_CODIGO=01` e hizo una venta a crédito de 10.00 para un
cliente de prueba y un abono completo de 10.00 desde la pantalla
de CxC. Las respuestas HTTP fueron 200; la BD terminó con dos ventas, stock
18, una cuenta `PAGADA`, saldo 0 y un pago de 10.00. El ensayo usó
`SYNC_ENABLED=False`, así que demuestra UI/FIFO/CxC locales y no acredita el
transporte de esas ventas a cloud. La impresora y e-CF físicos no se ensayaron.
Capturas y scripts del recorrido están fuera de Git bajo
`C:\Proyectos\_lab_a09_rig_20260924`.

Un primer pull de configuración quedó bloqueado: el plan Empresarial de
`staging_demo` anuncia e-CF activo, pero la configuración cloud no entrega un
`emisor_activo` local. Para continuar el ensayo de transporte se añadió un
emisor **ficticio solo en la BD POS desechable**; no se cambió el plan ni la
configuración cloud. El segundo pull aplicó la configuración. Una instalación
real con e-CF activo requiere aprovisionar su emisor antes de ese pull, o una
decisión de plan/override aprobada. No se acredita operación fiscal.

En caja sintética, el primer evento de apertura para `admin` fue rechazado
por integridad: ese usuario ya tenía un turno abierto en staging. El evento
quedó `ERROR`, sin ACK; se descartó únicamente en la BD local de prueba.
Con una cuenta de servicio sin turno abierto, la apertura y el cierre
confirmaron. El cloud conservó exactamente dos eventos y un turno `CERRADO`
con fondo 100, contado 100 y diferencia 0; reenviar el mismo `event_id` de
apertura devolvió `DUPLICADO` sin crear otro turno. Esto acredita transporte,
ACK e idempotencia de caja sintética, no una venta/cobro real en pantalla.

En la API portal de staging, un cliente sintético pasó de límite 100 a 200;
la fila cambió, pero había **0 eventos** de auditoría para ese cliente. Esto
reprodujo CLI-011 en el backend desplegado y motivó la corrección del PR #27.
Tras desplegar `86adf7f`, el portal cambió el límite de 200 a **250** y el
precio de un producto sintético de 10 a **11**. Ambas respuestas fueron 200 y
quedó **exactamente un** evento `audit.event.v1` `PORTAL_API` por mutación en
`tnt_staging_demo`, con `tenant_key=staging_demo`, actor `admin` y resultado
`SUCCEEDED`. En `tnt_staging_royalplast` siguen **0 productos** y **0 eventos**
de esas dos acciones. Un POS local limpio descargó 1 producto + 1 cliente,
aplicó precio 11/límite 250, dejó los dos diferidos previos en `RESUELTO` y el
pull siguiente trajo 0 productos/0 clientes. Chrome headless repitió login y
pantallas `/productos`/`/clientes` sobre el portal publicado. Las rutas locales
de clientes aún no tienen la misma garantía CT-01.

## Gates pendientes

| Gate | Estado al corte | Falta material |
| --- | --- | --- |
| G1 — candidato de código y datos | Parcial | Reconstruir el paquete del SHA documental final; corte de PRO/CLI y SEC-001; SHA/binario exactos y copias **actuales** de RP/SK. El ensayo de venta/crédito/abono/FIFO pasó en POS local desechable, no en instalaciones reales. |
| G2 — staging aceptado | Parcial | Al menos 24 h con arranque en frío y conciliación diaria; POS instalado real y nuevo, offline/reinicio, permisos/revocación, restore y matriz física. El rig sintético de >200 productos sí pasó. |
| G3 — producción cloud | No iniciado | Pase explícito, digest/manifiesto aprobado, backups/preflight del día, migración de todos los tenants, 60 min y dos ciclos por POS instalado. |
| G4 — RP y SK | No iniciado | Actualización por tienda, conciliación de dinero/stock/hechos, aceptación del operador y observación real. |

El plan Terraform de staging validó 0 altas, 1 actualización de formato de
líneas en una consulta de alerta y 0 destrucciones; no se aplicó. Los
recorridos pendientes no deben convertirse en PASS por CI verde ni por este
ensayo sintético.
