# Handoff C06.3 — Lista de visita y comunicación al operador

Estado: **BORRADOR LISTO, VISITA NO AUTORIZADA**. Fecha: **2026-09-23**.
Agente: B / Claude. Punto 6 del encargo C06: *"Preparar lista de visita y
comunicación al operador: `.env`, SECRET_KEY si corresponde, cambios visibles
RD$, logout, inactivos y cifras de arqueo; no habilitar funciones aún
incompatibles con la versión POS instalada."*

Este documento no autoriza ni programa ninguna visita — es el material que
queda listo para cuando se autorice. No se ejecutó nada de esto contra un
cliente real; es una compilación contra el inventario/handoffs vigentes de
`integration/cierre-prod-A06-C04-C05@9e6fb996` más los hallazgos propios de
C06.1/C06.2.

**Gate previo a cualquier visita — no negociable:** el candidato actual **no
migra** sobre ninguna instalación real (`permisos.0011_ct02_identidad_
revisiones` falla siempre que haya 2+ roles — ver
`C06.1-paquete-windows-laboratorio-parte2.md`). El usuario confirmó que Codex
ya está corrigiendo esa migración. **No programar visita hasta que exista un
SHA nuevo con esa migración corregida y repetido el ensayo de C06.1 sobre
esa versión** (migrar 22-32 completo, no solo confirmar que 0011 ya no
revienta).

## Parte 1 — Comunicación al operador (lenguaje llano, para el dueño/cajera)

Qué se ve distinto después de esta actualización, y qué NO cambia:

| Cambio | Qué van a notar | Qué decirles |
| --- | --- | --- |
| Moneda `$` → `RD$` | Todo el sistema (pantalla, tickets, reportes) va a mostrar `RD$` en vez de `$` | Es solo el símbolo, los montos no cambian. Avisar ANTES de la visita, no el mismo día — es lo único 100% visible en cada pantalla (`OPS-RD-MONEDA`). |
| Cierre de caja ya no es automático | El sistema ya no cierra el turno solo a cierta hora; alguien tiene que correr el cierre manualmente | Quien cuadra caja debe saber que ahora **hay que ejecutar el cierre a mano** al final del turno — si nadie lo hace, el turno queda abierto (`RPT-005`, ya desplegado antes, revalidar que lo saben). |
| Sesión con límite absoluto | Si alguien deja la sesión abierta muchas horas, el sistema la cierra sola aunque haya estado usándola (antes solo expiraba por inactividad) | No es un error — es a propósito. Si les pasa a media venta, que vuelvan a entrar; no perdieron nada, la venta se guarda al confirmar, no antes. |
| Productos/categorías inactivos | La lista de productos por defecto ya no muestra los inactivos; hay un filtro "Activos / Inactivos / Todos" | Si buscan un producto y no aparece, revisar el filtro antes de asumir que se borró — nada se borra, se desactiva (`DEC-INACTIVOS`). |
| Cobrar sin turno abierto sigue permitido | Nada cambia aquí — se aclara porque alguien podría pensar que ahora es obligatorio abrir turno primero | Sigue igual que siempre: se puede cobrar efectivo sin turno, y el arqueo lo distingue (`CAJA-002`). |

**No anunciar todavía** (features reales pero incompletas — anunciarlas antes
de tiempo genera reportes de "bug" sobre algo que nunca se prometió):

- **Edición de cliente sin conexión**: a diferencia de producto/categoría
  (que sí tienen conflicto/resolución con el cloud), **editar un cliente
  offline no tiene todavía el mismo mecanismo de protección** — hay una
  contención parcial del lado cloud (`CLI-004`, `DIFERIDO_EXPLICITO`,
  contenido en `eaefc1b`, pero sin cola/CAS local). Instrucción concreta para
  el operador: **no editar el mismo cliente desde dos sucursales/PCs a la vez
  mientras esto no cierre.**
- **Sincronización silenciosa**: el servicio de sync puede estar reintentando
  el mismo error por días sin que `sc query POSFifoSync` lo muestre (hallazgo
  de `C06.2-revision-cliente-windows-portal.md`) — no hay alerta automática
  todavía. Ver checklist técnico abajo.

## Parte 2 — Checklist técnico de la visita (para quien ejecuta, humano o agente)

Este checklist es específico de lo que C06 verificó; **no reemplaza**
`docs/runbooks/ACTUALIZACION_CLIENTE_EXISTENTE.md` (que ya tiene su propia
"hoja de registro" para antes/después de sync) — se usa junto a él.

**Antes de salir hacia el cliente:**

1. Confirmar que el SHA candidato ya tiene corregida `permisos.0011` (gate de
   arriba) y que se repitió `manage.py migrate --plan` limpio de punta a
   punta en laboratorio, no solo que pasó de la 0011.
2. Repetir el ensayo de wheelhouse/paquete de `C06.1-paquete-windows-
   laboratorio.md` contra ese SHA nuevo (los hashes de esta entrega ya no
   sirven si el SHA cambió).
3. Llevar el paquete + wheelhouse en medio físico — el contrato es offline,
   no depender de internet en el local del cliente.

**En sitio, antes de tocar el servicio (preflight, ver
`deploy/preflight_actualizar.py` de C01):**

4. `verificar_instalacion` — capturar el resultado ANTES de tocar nada.
5. `verificar_sync --dias=7` (no solo `--dias=1`: el hallazgo de C06.2 es que
   un fallo puede acumularse por días sin alerta) — anotar cursores
   bloqueados, huecos de numeración, eventos sin confirmar.
6. Confirmar cuántas migraciones están realmente pendientes para **esta**
   tienda específica — C06.1 midió 32 contra una copia de BD de desarrollo;
   **cada tienda real puede tener un número distinto**, no asumir ninguna
   cifra fija (esto es literalmente lo que el encargo pide no asumir).
7. Backup completo antes de parar servicios: BD (`pg_dump`, ya cubierto por
   `actualizar.bat`), código/config (ensayado en C06.1 parte 1) y `media/`.

**Después de actualizar:**

8. `verificar_sync` de nuevo — comparar contra el paso 5.
9. Prueba de humo: venta + impresión + anulación (runbook existente, paso 9).
10. Confirmar visualmente `RD$` en pantalla, ticket y un reporte/PDF — es el
    único cambio que el dueño va a notar sin que se lo señalen.
11. Recordarle a quien cierra caja que el cierre ahora es manual (Parte 1).
12. **No** mencionar la edición de cliente offline como "ya arreglada".

## Qué falta para que esta lista deje de ser un borrador

- El gate de `permisos.0011` (arriba) — bloqueante, en manos de Codex ahora.
- Repetir C06.1 completo (wheelhouse + 32→0 migraciones sin cortes) contra el
  SHA corregido.
- Autorización explícita para la visita en sí — este documento no la pide ni
  la asume.

## Archivos

Solo este handoff. Cero cambios de código; es síntesis de inventario/handoffs
ya existentes (`INVENTARIO.md`, `BUGS.md`, `C06.1-*`, `C06.2-*`) más el
punto 6 del encargo, no información nueva de código.
