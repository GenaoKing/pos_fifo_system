# Handoff C06.1 — Ensayo de migración contra copias reales de RP y SK

Estado: **EVIDENCIA NUEVA, SIN BLOQUEANTES ENCONTRADOS**. Fecha: **2026-09-24**.
Agente: B / Claude. Responde a la Etapa 0 de `OBJETIVO_RELEASE_CLIENTES.md`
("copias representativas RP/SK") y al punto 1-2 del encargo original de C06,
usando datos reales (no sintéticos) por primera vez.

**Origen de los dumps**: `docs/dumps/royal-20-06-26.dump` (Royal Plast, dbname
original `royal_plastic_pos`, 2026-06-20) y `docs/dumps/sk_cutover.dump` (SK
Performance, dbname original `pos_autorepuestos`, snapshot del cutover
2026-06-23). Ambos ya estaban en el repo (gitignorados, `docs/dumps/`), el
usuario confirmó su uso. **No se leyó `docs/sk/env_cliente.bat` ni ningún
archivo con credenciales/config reales de esas rutas** — no hacía falta:
la restauración usa el Postgres local de este host con credenciales de
desarrollo, no las de producción.

**Manejo de datos sensibles**: restaurados en BDs descartables
(`pos_c06_rp_lab`, `pos_c06_sk_lab`), nunca en `pos_fifo_db` ni en ninguna BD
compartida. Este documento reporta solo **conteos agregados**; ningún nombre,
cédula, teléfono o dato personal se imprimió en ningún comando ni se copia
aquí.

## Resultado — ambas migran limpio, con una cifra muy distinta de "11"

| Tienda | Migraciones pendientes | Resultado | Roles/asignaciones tras migrar |
| --- | --- | --- | --- |
| Royal Plast (`royal-20-06-26`) | **78** | 78/78 OK | 2 roles, 2 asignaciones — cloud_id únicos y no nulos |
| SK Performance (`sk_cutover`) | **115** | 115/115 OK | 2 roles, 3 asignaciones — cloud_id únicos y no nulos |

Ninguna de las dos se detuvo en `permisos.0011` ni en ninguna otra migración
— el fix de Codex se sostiene contra datos reales, no solo contra el
escenario mínimo del test. **78 y 115 son cifras muy distintas entre sí y de
las 32 que dio mi copia de dev** — confirma con más fuerza todavía la
advertencia del encargo de no asumir un número fijo de migraciones por
tienda. Una ventana de actualización real necesita presupuestar tiempo para
la cifra más alta observada (115), no un promedio.

`manage.py check` limpio en ambas. Conteos post-migración (agregados):
Royal Plast 325 productos / 4 clientes / 370 ventas, sucursal `01`; SK 21
productos / 2 clientes / 194 ventas.

## Hallazgo — el dump de SK no tiene fila de `Sucursal`

`verificar_instalacion` sobre la copia de SK: `Sucursal: (NO RESUELTA)`,
`Negocio: (sin negocio)`, cae al modo de flags legacy y reporta `ecf`,
`etiquetas_zebra` y `financiacion` apagados. **Esto no es necesariamente el
estado actual de la tienda real** — el archivo se llama `sk_cutover` y su
fecha (2026-06-23) coincide exactamente con la fecha de go-live registrada en
memoria del proyecto: es plausible que sea la foto tomada **antes** de
`crear_sucursal`/`bootstrap_negocio` en la secuencia de instalación (ver
`INSTALACION_CLIENTE_NUEVO.md`), no una foto de la BD tal como opera hoy. No
se puede confirmar sin acceso a la BD real de SK, que está fuera de alcance
aquí. **No se trata como bloqueante**: la instalación no falla, cae a un modo
degradado documentado (`RESULTADO: instalacion sana` con avisos) en vez de
romperse — pero si la copia real que se use en staging/producción resulta
tener el mismo hueco, hay que correr `crear_sucursal` antes de continuar,
exactamente como ya advierte el runbook.

Los módulos apagados (`ecf`, `etiquetas_zebra`, `financiacion`) coinciden con
el corte de alcance de `OBJETIVO_RELEASE_CLIENTES.md` — no hace falta
encenderlos para este release.

## Qué no se repitió aquí (ya probado con datos sintéticos, ver relevo anterior)

El mecanismo de backup/restore (`pg_dump`/`pg_restore`, pérdida total
simulada, verificación de integridad) ya se probó dos veces en
`C06.1-relevo-d69c73e-ensayo-completo.md` — no es sensible al contenido de
los datos, así que no se repitió por tienda. Lo nuevo de este documento es
específicamente si el candidato **migra** limpio sobre datos reales de cada
tienda, que sí depende del contenido y es lo que estaba sin probar.

## Archivos

Solo este handoff. Nada de código tocado; las BDs de laboratorio y los logs
de migración quedan fuera de Git en `_lab_pos_c06_1_20260923`.

## Rollback

`dropdb pos_c06_rp_lab pos_c06_sk_lab` deja el host como estaba. Los dumps
originales en `docs/dumps/` no se modificaron (`pg_restore` es de solo
lectura sobre el archivo fuente).
