# Handoff C06.1 — Relevo completo sobre `d69c73e` (PASS)

Estado: **CANDIDATO LOCAL — NO PROMOTABLE, ENSAYO COMPLETO PASS**. Fecha:
**2026-09-24**. Agente: B / Claude. Responde punto por punto al "Relevo
C06.1" de
`docs/handoffs/cierre_prod/INTEGRACION-TOTAL-2026-09-24.md` (worktree de
integración). No reemplaza `C06.1-paquete-windows-laboratorio.md` ni su
`-parte2.md` — los da por superados en el candidato nuevo.

## SHA y aislamiento

- Base: `integration/cierre-prod-A06-C04-C05@d69c73e791e590dd087ebeac44a8126dbb840224`.
  Confirmado `git merge-base --is-ancestor dec46a3 d69c73e` → OK.
- Worktree **nuevo** (no se rebaseó ni modificó el worktree fuente ni las
  ramas viejas C06.1/C06.2/C06.3): `C:\Proyectos\pos_fifo_system_c06_1_relevo`,
  rama `claude/cierre-prod-C06.1-relevo-d69c73e`.
- Laboratorio reusado (Codex confirmó que nadie lo tocó durante la
  integración): `C:\Proyectos\_lab_pos_c06_1_20260923\`. Venv nuevo para esta
  corrida: `C:\Proyectos\.venvs\pos_c06_1_relevo_install_20260924` (conda
  compartido intacto).

## 1-2. Paquete reconstruido con la herramienta madurada por Codex

`scripts/c06_construir_paquete.py` en este SHA ya no es mi versión original:
Codex la endureció (`58f20a6`) con extracción de tar segura
(`extraer_tar_seguro`, rechaza paths fuera del staging o tipos no regulares),
`copiar_wheelhouse_verificado` (resuelve el lock con `pip download --no-index
--require-hashes` antes de copiar, no confía en que la carpeta ya esté
correcta) y ZIP reproducible (`source_date_epoch` fijo desde el commit).
Usé esa versión, sin editarla:

```
python scripts\c06_construir_paquete.py --repo <worktree> \
  --sha d69c73e791e590dd087ebeac44a8126dbb840224 \
  --wheelhouse <lab>\wheelhouse\windows-py311 --lab <lab> \
  --portal-ref-branch integration/cierre-prod-C04-admin \
  --portal-ref-sha a9d960e3736067dfd3e2de0f1e3dff8b3e2e32e6
```

- `requirements.txt` SHA-256: `211a4ba5a93640c7554fa7ea9ff9fa7e1aa8e529eb001d116aa01e4a2fc00081`
  — **idéntico** al baseline A01 y a la corrida anterior. Sin drift de
  dependencias entre `9e6fb99` y `d69c73e`.
- Wheelhouse reusado (36 wheels, mismo lock) — `pip download --no-index`
  volvió a resolverlo sin red antes de empaquetar, confirmando que sigue
  completo para este lock exacto.
- ZIP: `pos_fifo_system_C06.1_d69c73e791e5.zip`, 37.195.547 bytes, SHA-256
  `3ba925d4d120e47e730ee3959d7e8fd46925c98f363e0de05a1ba2b892a82f65`.
  Manifiesto: `MANIFIESTO_C06.1_d69c73e791e5.json` en el laboratorio.
- Instalación offline en venv limpio (`--no-index --require-hashes`):
  OK, `pip check` → `No broken requirements found.`

## 3-4. Ensayo completo de migraciones desde la copia restaurada (no solo desde la 22)

Reusé el dump de `pos_fifo_db` ya documentado en la parte 2 anterior
(`pos_fifo_db` real **nunca** se tocó; Codex confirmó lo mismo durante la
integración). `pos_c06_1_lab` seguía exactamente en el estado limpio
pre-batch (`permisos.0008` como último aplicado, verificado antes de
empezar) — **no partí desde la migración 22**, partí desde cero otra vez.

```
manage.py migrate --settings=config.settings_development --plan
# 32 migraciones pendientes (mismo numero que antes; el fix no agrega/quita migraciones)
manage.py migrate --settings=config.settings_development
# Applying permisos.0011_ct02_identidad_revisiones... OK
# ... 32/32 OK, ninguna interrumpida
```

Verificado por ORM, no solo por el mensaje `OK` del comando:

- `Rol.cloud_id`: 2 filas, **ambas distintas y no nulas**
  (`6750fcde-...`, `14873619-...`).
- `AsignacionRol.cloud_id`: 4 filas, **las 4 distintas y no nulas**.
- `productos.Producto`: 89 (igual que antes de migrar). `ventas.Venta`: 45.
- `manage.py check`: `System check identified no issues (0 silenced)`.
- `manage.py verificar_instalacion` (con `SUCURSAL_CODIGO=SD-001`, el código
  real de esta copia — corregido desde el `99` sintético de la parte 2, que
  generaba un warning de sucursal no resuelta sin ser un defecto del
  candidato): **`RESULTADO: instalacion sana.`** Reveló que esta copia de
  desarrollo es en realidad de **Royal Plast EIRL** (`SD-001`) — dato interno
  del entorno de desarrollo, no un cliente contactado ni datos de producción.
- `manage.py verificar_sync --dias=120`: **1 objeto sin evento, 1 evento
  DESCARTADO, 1 PENDIENTE con 737 horas de antigüedad, sin huecos de
  numeración.** No es un estado sintético "limpio" — es deuda real de una BD
  de desarrollo con historia, buena señal de que el ensayo no es un caso de
  juguete. Nada de esto lo causa el candidato: son eventos previos a esta
  corrida.

## Backup/restore con datos reales — ensayado también para una actualización EXITOSA

La parte 2 anterior solo había ensayado rollback después de un fallo. Esta
vez, además, se ensayó el caso de éxito: respaldar **después** de migrar y
restaurar sobre pérdida total.

```
pg_dump ... pos_c06_1_lab   # backup POST-migracion (450.857 bytes)
DROP DATABASE pos_c06_1_lab; createdb ...
pg_restore ... --no-owner --no-privileges -j 4 <dump>
```

Verificado tras restaurar: `showmigrations permisos` → las 11 migraciones de
esa app **siguen aplicadas** (incluida la 0011); `Rol`/`AsignacionRol.cloud_id`
siguen únicos y no nulos; productos=89, ventas=45 — idéntico al estado
post-migración, no al estado pre-migración. El backup/restore preserva el
candidato migrado, no solo el estado viejo.

## Veredicto contra el punto 5 del "Relevo C06.1"

> *"PASS requiere todo el ensayo con evidencia nueva sobre ese SHA. Un test
> verde de 0011 o un paquete reproducible por sí solos no completan C06.1."*

Se cumplen ambos: wheelhouse/paquete reconstruido y verificado **y** las 32
migraciones completas aplicadas de punta a punta sobre una copia real
restaurada, con identidad/conteos/backup/restore verificados en los dos
sentidos (antes de migrar y después de migrar). **C06.1 queda PASS sobre
`d69c73e`.**

## Qué sigue sin cubrir (mismos límites que antes, no resueltos aquí)

- BD real de cliente (RP/SK), NSSM real, Windows limpio físico: bloqueado por
  autorización, igual que siempre.
- Identidad inicial a fondo y multi-tenant desde el cliente (mencionado en el
  handoff de integración como pendiente de C06) — no es parte de este
  relevo, que es específicamente el paquete/BD.
- El gate que fijé en `C06.3-lista-visita-comunicacion-operador.md` ("no
  programar visita hasta que se repita C06.1 completo sobre el SHA
  corregido") **queda satisfecho por este documento** — la visita real sigue
  sin autorizarse, pero el bloqueo técnico ya no aplica.

## Archivos

Solo este handoff. `scripts/c06_construir_paquete.py` no se modificó (se usó
tal cual lo dejó Codex). Ningún archivo de `apps/permisos` ni de ningún otro
dominio de Codex se tocó.

## Rollback

Igual que las corridas anteriores: todo vive en un worktree/rama propios y en
el laboratorio fuera de Git. `pos_fifo_db` real nunca se tocó en ningún punto
de este relevo.
